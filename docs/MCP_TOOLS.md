# MCP Tool Catalogue

_Auto-generated by `scripts/gen_tool_docs.py` — 241 tools total._

Every tool is prefixed `cutmaster_` and returns a string (error text or JSON / success message). Tools are grouped by the module that registers them — see [ARCHITECTURE.md](ARCHITECTURE.md) for the responsibility model.

## `cutmaster/core/snapshot.py`

| Tool | Description |
|---|---|
| `cutmaster_snapshot_project` | Export the current project to a .drp snapshot (non-destructive backup). |

## `cutmaster/media/ffmpeg_audio.py`

| Tool | Description |
|---|---|
| `cutmaster_extract_timeline_audio` | Extract the current timeline's audio to a WAV via ffmpeg (source files). |

## `cutmaster/media/frame_math.py`

| Tool | Description |
|---|---|
| `cutmaster_frame_to_seconds` | Convert an absolute timeline frame to seconds (relative to timeline start). |
| `cutmaster_seconds_to_frame` | Convert timeline-seconds to an absolute timeline frame on the current timeline. |

## `cutmaster/media/vfr.py`

| Tool | Description |
|---|---|
| `cutmaster_detect_vfr` | Detect whether a media file is variable frame rate (VFR). |

## `cutmaster/resolve_ops/source_mapper.py`

| Tool | Description |
|---|---|
| `cutmaster_resolve_timeline_time_to_source` | Map a timeline time (seconds) to the source clip + source frame playing then. |

## `cutmaster/resolve_ops/subclips.py`

| Tool | Description |
|---|---|
| `cutmaster_append_subclips_with_ranges` | Append a batch of ranged subclips to the current timeline. |

## `intelligence/color_assist.py`

| Tool | Description |
|---|---|
| `cutmaster_color_assist` | Get AI-powered color correction suggestions for the current frame. |
| `cutmaster_match_to_reference` | Match the current frame's color to a reference image using AI. |

## `intelligence/timeline_critique.py`

| Tool | Description |
|---|---|
| `cutmaster_suggest_markers` | AI analyzes the timeline and suggests where markers should be placed. |
| `cutmaster_timeline_critique` | Get AI editorial feedback on the current timeline. |
| `cutmaster_visual_continuity_check` | Check visual continuity by analyzing frames at each cut point. |

## `intelligence/vision.py`

| Tool | Description |
|---|---|
| `cutmaster_analyze_frame` | Analyze the current timeline frame using Gemini vision AI. |
| `cutmaster_compare_frames` | Compare the current frame to a reference image. |
| `cutmaster_describe_shot` | Get a cinematography-focused description of the current frame. |
| `cutmaster_frame_info` | Get metadata about the current frame without AI analysis. |
| `cutmaster_ocr_frame` | Extract text from the current frame using Gemini OCR. |

## `tools/color.py`

| Tool | Description |
|---|---|
| `cutmaster_add_node` | Add a new node to the node graph. |
| `cutmaster_apply_grade_from_drx` | Apply a grade from a .drx file to timeline items. |
| `cutmaster_copy_grades` | Copy color grades from one timeline item to others. |
| `cutmaster_get_cdl` | Get the CDL values of a timeline item. |
| `cutmaster_get_lut` | Get the LUT path applied to a node. |
| `cutmaster_get_node_graph` | Get the node graph for a timeline item. |
| `cutmaster_grab_still` | Grab a still from the current frame in the timeline. |
| `cutmaster_set_cdl` | Set CDL values on a timeline item. |
| `cutmaster_set_lut` | Apply a LUT to a color node. |
| `cutmaster_set_node_cache_mode` | Set the cache mode on a color node. |
| `cutmaster_set_node_enabled` | Enable or disable a color node. |
| `cutmaster_set_node_label` | Set the label on a color node. |

## `tools/fairlight.py`

| Tool | Description |
|---|---|
| `cutmaster_get_audio_track_info` | Get information about all audio tracks in the current timeline. |
| `cutmaster_insert_audio_at_playhead` | Insert an audio file at the playhead position on the current audio track. |
| `cutmaster_set_audio_track_volume` | Set the volume of an audio track. |
| `cutmaster_voice_isolation` | Apply voice isolation to an audio track (Studio only). |

## `tools/fusion.py`

| Tool | Description |
|---|---|
| `cutmaster_add_fusion_comp` | Add a new Fusion composition to a timeline item. |
| `cutmaster_delete_fusion_comp` | Delete a Fusion composition by name. |
| `cutmaster_export_fusion_comp` | Export a Fusion composition to a .comp file. |
| `cutmaster_fusion_add_tool` | Add a tool/node to a Fusion composition. |
| `cutmaster_fusion_connect` | Connect two tools in a Fusion composition. |
| `cutmaster_fusion_delete_tool` | Delete a tool from a Fusion composition. |
| `cutmaster_fusion_end_undo` | End an undo group in a Fusion composition. |
| `cutmaster_fusion_find_tool` | Find a tool in a Fusion composition by name. |
| `cutmaster_fusion_get_comp_info` | Get information about a Fusion composition (frame range, current time). |
| `cutmaster_fusion_get_input` | Get the current value of an input on a Fusion tool. |
| `cutmaster_fusion_get_tool_list` | List all tools in a Fusion composition. |
| `cutmaster_fusion_render` | Render a Fusion composition. |
| `cutmaster_fusion_set_input` | Set an input value on a Fusion tool. |
| `cutmaster_fusion_undo` | Start an undo group in a Fusion composition. |
| `cutmaster_get_fusion_comp_count` | Get the number of Fusion compositions on a timeline item. |
| `cutmaster_import_fusion_comp` | Import a Fusion composition from a .comp file. |
| `cutmaster_insert_fusion_comp_into_timeline` | Insert a new Fusion composition clip at the playhead in the timeline. |
| `cutmaster_load_fusion_comp` | Load/activate a Fusion composition by name. |
| `cutmaster_rename_fusion_comp` | Rename a Fusion composition. |

## `tools/gallery.py`

| Tool | Description |
|---|---|
| `cutmaster_delete_stills` | Delete stills from the current album. |
| `cutmaster_export_stills` | Export stills from the current album to files. |
| `cutmaster_get_current_album` | Get the currently selected still album. |
| `cutmaster_import_stills` | Import stills into the current album. |
| `cutmaster_list_gallery_albums` | List all still albums in the gallery. |
| `cutmaster_list_power_grade_albums` | List all power grade albums. |
| `cutmaster_list_stills` | List all stills in the current album. |
| `cutmaster_set_current_album` | Set the current still album by index. |
| `cutmaster_set_still_label` | Set the label on a still in the current album. |

## `tools/graph.py`

| Tool | Description |
|---|---|
| `cutmaster_add_group_graph_node` | Add a node to a color group's pre/post-clip graph. |
| `cutmaster_create_color_group` | Create a new color group. |
| `cutmaster_delete_color_group` | Delete a color group by name. |
| `cutmaster_get_color_group_clips` | Get all timeline items assigned to a color group. |
| `cutmaster_get_post_clip_graph` | Get the post-clip node graph for a color group. |
| `cutmaster_get_pre_clip_graph` | Get the pre-clip node graph for a color group. |
| `cutmaster_set_group_graph_lut` | Apply a LUT to a node in a color group's pre/post-clip graph. |

## `tools/interchange.py`

| Tool | Description |
|---|---|
| `cutmaster_export_aaf` | Export the current timeline as AAF (Advanced Authoring Format). |
| `cutmaster_export_csv` | Export the current timeline as CSV. |
| `cutmaster_export_edl` | Export the current timeline as an EDL (Edit Decision List). |
| `cutmaster_export_fcpxml` | Export the current timeline as FCPXML (Final Cut Pro XML). |
| `cutmaster_export_otio` | Export the current timeline as OTIO (OpenTimelineIO). |
| `cutmaster_import_timeline_file` | Import a timeline from a file (AAF, EDL, XML, FCPXML, OTIO). |

## `tools/layout.py`

| Tool | Description |
|---|---|
| `cutmaster_delete_layout_preset` | Delete a layout preset. |
| `cutmaster_export_burn_in_preset` | Export a burn-in preset to a file. |
| `cutmaster_export_layout_preset` | Export a layout preset to a file. |
| `cutmaster_get_keyframe_mode` | Get the current keyframe mode. |
| `cutmaster_import_burn_in_preset` | Import a burn-in preset from a file. |
| `cutmaster_import_layout_preset` | Import a layout preset from a file. |
| `cutmaster_load_layout_preset` | Load a UI layout preset. |
| `cutmaster_refresh_lut_list` | Refresh the project's LUT list (re-scan LUT directories). |
| `cutmaster_save_layout_preset` | Save the current UI layout as a preset. |
| `cutmaster_set_keyframe_mode` | Set the keyframe mode. |
| `cutmaster_update_layout_preset` | Update an existing layout preset with the current UI state. |

## `tools/lut_registry.py`

| Tool | Description |
|---|---|
| `cutmaster_get_lut_install_guide` | Get step-by-step instructions for installing the LUT for a specific camera. |
| `cutmaster_install_lut_file` | Install a downloaded LUT file into the CutMaster LUT library. |
| `cutmaster_lut_library_status` | Show the status of the LUT library — what's installed and what's available. |
| `cutmaster_scan_lut_folder` | Scan the Resolve LUT folders and show all installed LUTs. |

## `tools/markers.py`

| Tool | Description |
|---|---|
| `cutmaster_add_clip_marker` | Add a marker to a media pool clip. |
| `cutmaster_add_item_marker` | Add a marker to a timeline item (clip marker, frame offset relative to clip start). |
| `cutmaster_add_timeline_marker` | Add a marker to the current timeline. |
| `cutmaster_delete_item_marker_at_frame` | Delete a marker on a timeline item at a specific frame offset. |
| `cutmaster_delete_item_markers_by_color` | Delete all markers of a specific color on a timeline item. |
| `cutmaster_delete_timeline_marker_at_frame` | Delete a timeline marker at a specific frame. |
| `cutmaster_delete_timeline_markers_by_color` | Delete all timeline markers of a specific color. |
| `cutmaster_get_clip_markers` | Get all markers on a media pool clip. |
| `cutmaster_get_item_markers` | Get all markers on a timeline item. |
| `cutmaster_get_timeline_markers` | Get all markers on the current timeline. |
| `cutmaster_update_timeline_marker_custom_data` | Update the custom data on a timeline marker. |

## `tools/media_pool.py`

| Tool | Description |
|---|---|
| `cutmaster_clear_clip_color` | Clear the clip color label from a media pool clip. |
| `cutmaster_create_bin` | Create a new bin (folder) in the media pool. |
| `cutmaster_delete_bins` | Delete bins by name. Bins must be empty. |
| `cutmaster_delete_clips` | Delete clips from the media pool by name. |
| `cutmaster_export_metadata` | Export media pool metadata to a CSV file. |
| `cutmaster_get_clip_info` | Get all properties and metadata for a media pool clip. |
| `cutmaster_get_current_bin` | Get the currently selected bin in the media pool. |
| `cutmaster_import_media` | Import media files into the media pool. |
| `cutmaster_link_proxy_media` | Link proxy media to a clip. |
| `cutmaster_list_bins` | List all bins (folders) in the media pool with clip counts. |
| `cutmaster_list_clips` | List clips in a bin. Defaults to the current bin. |
| `cutmaster_move_bins` | Move bins to a different parent bin. |
| `cutmaster_move_clips` | Move clips to a different bin. |
| `cutmaster_refresh_bins` | Refresh the media pool folder structure. |
| `cutmaster_relink_clips` | Relink clips to a new media path. |
| `cutmaster_replace_clip` | Replace a clip's media with a different file. |
| `cutmaster_search_clips` | Search for clips by name substring (case-insensitive). |
| `cutmaster_set_clip_color` | Set the clip color label on a media pool clip. |
| `cutmaster_set_clip_metadata` | Set a metadata field on a media pool clip. |
| `cutmaster_set_clip_property` | Set a property on a media pool clip. |
| `cutmaster_set_current_bin` | Set the current bin by name or path (e.g. 'Master/Footage/Day1'). |
| `cutmaster_unlink_clips` | Unlink clips from their media files (make offline). |
| `cutmaster_unlink_proxy_media` | Unlink proxy media from a clip. |

## `tools/media_storage.py`

| Tool | Description |
|---|---|
| `cutmaster_browse_storage` | List sub-folders in a media storage path. |
| `cutmaster_import_from_storage` | Import media files from storage directly into the media pool. |
| `cutmaster_list_files_in_storage` | List files in a media storage path. |
| `cutmaster_list_volumes` | List all mounted media storage volumes. |
| `cutmaster_reveal_in_storage` | Reveal a file or folder in the Media Storage browser. |

## `tools/project.py`

| Tool | Description |
|---|---|
| `cutmaster_archive_project` | Archive a project to a .dra file with optional media. |
| `cutmaster_close_project` | Close the current project (returns to Project Manager). |
| `cutmaster_create_project` | Create a new project with the given name. |
| `cutmaster_create_project_folder` | Create a new folder in the current Project Manager location. |
| `cutmaster_delete_project` | Delete a project by name. The project must not be currently open. |
| `cutmaster_delete_project_folder` | Delete a folder in the Project Manager. Must be empty. |
| `cutmaster_export_project` | Export a project to a .drp file. |
| `cutmaster_get_current_database` | Get the currently active database. |
| `cutmaster_get_current_project` | Get the name and details of the currently open project. |
| `cutmaster_get_project_setting` | Get a project setting by key, or all settings if key is empty. |
| `cutmaster_get_version` | Get DaVinci Resolve product name, version, and current page. |
| `cutmaster_goto_parent_folder` | Navigate up one level in the Project Manager. |
| `cutmaster_goto_root_folder` | Navigate to the root of the Project Manager. |
| `cutmaster_import_project` | Import a project from a .drp file. |
| `cutmaster_list_databases` | List all available databases. |
| `cutmaster_list_project_folders` | List folders in the current Project Manager location. |
| `cutmaster_list_projects` | List all projects in the current database folder. |
| `cutmaster_open_project` | Open an existing project by name. |
| `cutmaster_open_project_folder` | Navigate into a Project Manager folder. |
| `cutmaster_restore_project` | Restore a project from a .dra archive. |
| `cutmaster_save_project` | Save the current project. |
| `cutmaster_set_project_setting` | Set a project setting. |
| `cutmaster_switch_database` | Switch to a different database. |
| `cutmaster_switch_page` | Switch to a Resolve page. |

## `tools/render.py`

| Tool | Description |
|---|---|
| `cutmaster_add_render_job` | Add the current timeline to the render queue with current settings. |
| `cutmaster_delete_all_render_jobs` | Clear all render jobs from the queue. |
| `cutmaster_delete_render_job` | Delete a render job from the queue. |
| `cutmaster_export_render_preset` | Export a render preset to a file. |
| `cutmaster_get_render_codecs` | List available codecs for a render format. |
| `cutmaster_get_render_format_and_codec` | Get the current render format and codec. |
| `cutmaster_get_render_formats` | List all available render formats. |
| `cutmaster_get_render_job_status` | Get the status of a specific render job. |
| `cutmaster_get_render_jobs` | List all render jobs in the queue. |
| `cutmaster_get_render_resolutions` | List available resolutions for a format/codec combination. |
| `cutmaster_get_render_settings` | Get all current render settings. |
| `cutmaster_import_render_preset` | Import a render preset from a file. |
| `cutmaster_is_rendering` | Check if rendering is currently in progress. |
| `cutmaster_list_render_presets` | List all available render presets. |
| `cutmaster_load_render_preset` | Load a render preset by name. |
| `cutmaster_save_render_preset` | Save current render settings as a new preset. |
| `cutmaster_set_render_format_and_codec` | Set the current render format and codec. |
| `cutmaster_set_render_settings` | Set render settings. |
| `cutmaster_start_render` | Start rendering jobs in the queue. |
| `cutmaster_stop_render` | Stop the current render. |

## `tools/scripting.py`

| Tool | Description |
|---|---|
| `cutmaster_execute_lua` | Execute a Lua script in the Fusion environment. |
| `cutmaster_execute_python` | Execute arbitrary Python code with DaVinci Resolve API objects in scope. |

## `tools/timeline_edit.py`

| Tool | Description |
|---|---|
| `cutmaster_create_compound_clip` | Create a compound clip from selected timeline items. |
| `cutmaster_create_fusion_clip` | Create a Fusion clip from selected timeline items. |
| `cutmaster_get_item_property` | Get a property from a timeline item. |
| `cutmaster_insert_fusion_generator` | Insert a Fusion generator into the current timeline at the playhead. |
| `cutmaster_insert_fusion_title` | Insert a Fusion title into the current timeline at the playhead. |
| `cutmaster_insert_generator` | Insert a generator into the current timeline at the playhead. |
| `cutmaster_insert_title` | Insert a title into the current timeline at the playhead. |
| `cutmaster_list_timeline_items` | List all items on a timeline track. |
| `cutmaster_set_clip_enabled` | Enable or disable a timeline item. |
| `cutmaster_set_composite_mode` | Set the composite/blend mode on a timeline item. |
| `cutmaster_set_crop` | Set crop values on a timeline item. |
| `cutmaster_set_item_property` | Set a property on a timeline item. |
| `cutmaster_set_opacity` | Set the opacity of a timeline item (0.0 to 100.0). |
| `cutmaster_set_speed` | Set the playback speed of a timeline item. |
| `cutmaster_set_transform` | Set transform properties on a timeline item in one call. |

## `tools/timeline_items.py`

| Tool | Description |
|---|---|
| `cutmaster_add_item_flag` | Add a flag to a timeline item. |
| `cutmaster_add_version` | Add a new grade version to a timeline item. |
| `cutmaster_assign_to_color_group` | Assign a timeline item to a color group. |
| `cutmaster_clear_item_flags` | Clear flags from a timeline item. |
| `cutmaster_delete_version` | Delete a grade version. |
| `cutmaster_finalize_take` | Finalize the current take (remove other takes, keep selected). |
| `cutmaster_get_current_video_item` | Get the timeline item currently under the playhead. |
| `cutmaster_get_item_flags` | Get all flags on a timeline item. |
| `cutmaster_get_playhead_position` | Get the current playhead timecode. |
| `cutmaster_list_color_groups` | List all color groups in the current project. |
| `cutmaster_list_takes` | List all takes on a timeline item. |
| `cutmaster_list_versions` | List all grade versions on a timeline item. |
| `cutmaster_load_version` | Switch to a specific grade version. |
| `cutmaster_rename_version` | Rename a grade version. |
| `cutmaster_select_take` | Select a specific take on a timeline item. |
| `cutmaster_set_item_clip_color` | Set the clip color on a timeline item. |
| `cutmaster_set_playhead_position` | Set the playhead to a specific timecode. |

## `tools/timeline_mgmt.py`

| Tool | Description |
|---|---|
| `cutmaster_add_track` | Add a track to the current timeline. |
| `cutmaster_append_clips_to_timeline` | Append clips from the media pool to the end of the current timeline. |
| `cutmaster_create_timeline` | Create a new empty timeline. |
| `cutmaster_create_timeline_from_clips` | Create a timeline populated with the specified clips. |
| `cutmaster_delete_timelines` | Delete timelines by name. |
| `cutmaster_delete_track` | Delete a track from the current timeline. |
| `cutmaster_duplicate_timeline` | Duplicate the current timeline. |
| `cutmaster_export_timeline` | Export the current timeline to a file. |
| `cutmaster_get_current_timeline` | Get info about the currently active timeline. |
| `cutmaster_get_track_count` | Get the number of tracks of a given type. |
| `cutmaster_get_track_name` | Get the name of a track. |
| `cutmaster_import_timeline` | Import a timeline from a file (AAF, EDL, XML, FCPXML, OTIO). |
| `cutmaster_list_timelines` | List all timelines in the current project. |
| `cutmaster_set_current_timeline` | Switch to a timeline by name. |
| `cutmaster_set_timeline_name` | Rename the current timeline. |
| `cutmaster_set_track_enabled` | Enable or disable a track. |
| `cutmaster_set_track_lock` | Lock or unlock a track. |
| `cutmaster_set_track_name` | Rename a track. |

## `workflows/assembly.py`

| Tool | Description |
|---|---|
| `cutmaster_add_clips_to_track` | Add specific clips to a specific track on the current timeline. |
| `cutmaster_assembly_from_bin` | Create a timeline from all clips in a bin with track configuration. |
| `cutmaster_quick_assembly` | Create a timeline and add clips to it in one step. |

## `workflows/chroma_key.py`

| Tool | Description |
|---|---|
| `cutmaster_chroma_key` | Apply chroma key (green/blue screen removal) to timeline clips via Fusion DeltaKeyer. |

## `workflows/conform.py`

| Tool | Description |
|---|---|
| `cutmaster_conform_timeline` | Import a timeline file and relink to source media. |
| `cutmaster_relink_offline_clips` | Relink all offline clips in a bin to media at a new path. |
| `cutmaster_verify_timeline_media` | Verify that all clips in the current timeline are online. |

## `workflows/delivery.py`

| Tool | Description |
|---|---|
| `cutmaster_batch_deliver` | Queue multiple renders with different presets and start them all. |
| `cutmaster_quick_deliver` | One-command render: set format, configure output, queue, and start. |
| `cutmaster_render_status` | Check the status of all render jobs in the queue. |

## `workflows/grade.py`

| Tool | Description |
|---|---|
| `cutmaster_batch_apply_lut` | Apply a LUT to multiple timeline items at once. |
| `cutmaster_copy_grade_to_all` | Copy the grade from one clip to all other clips on the same track. |
| `cutmaster_list_cst_luts` | List all supported cameras and CST LUTs for the 6-node log grading workflow. |
| `cutmaster_quick_grade` | Apply a LUT and/or CDL to a timeline item in one step. |
| `cutmaster_setup_log_grade` | Set up the 6-node log footage grading structure on a clip or all clips. |

## `workflows/ingest.py`

| Tool | Description |
|---|---|
| `cutmaster_ingest_media` | Import media from a folder into the media pool with optional organisation. |
| `cutmaster_ingest_with_bins` | Import media and auto-organise into bins based on folder structure. |
