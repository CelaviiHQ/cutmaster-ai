# MCP Tool Catalogue

_Auto-generated by `scripts/gen_tool_docs.py` — 241 tools total._

Every tool is prefixed `celavii_` and returns a string (error text or JSON / success message). Tools are grouped by the module that registers them — see [ARCHITECTURE.md](ARCHITECTURE.md) for the responsibility model.

## `cutmaster/core/snapshot.py`

| Tool | Description |
|---|---|
| `celavii_snapshot_project` | Export the current project to a .drp snapshot (non-destructive backup). |

## `cutmaster/media/ffmpeg_audio.py`

| Tool | Description |
|---|---|
| `celavii_extract_timeline_audio` | Extract the current timeline's audio to a WAV via ffmpeg (source files). |

## `cutmaster/media/frame_math.py`

| Tool | Description |
|---|---|
| `celavii_frame_to_seconds` | Convert an absolute timeline frame to seconds (relative to timeline start). |
| `celavii_seconds_to_frame` | Convert timeline-seconds to an absolute timeline frame on the current timeline. |

## `cutmaster/media/vfr.py`

| Tool | Description |
|---|---|
| `celavii_detect_vfr` | Detect whether a media file is variable frame rate (VFR). |

## `cutmaster/resolve_ops/source_mapper.py`

| Tool | Description |
|---|---|
| `celavii_resolve_timeline_time_to_source` | Map a timeline time (seconds) to the source clip + source frame playing then. |

## `cutmaster/resolve_ops/subclips.py`

| Tool | Description |
|---|---|
| `celavii_append_subclips_with_ranges` | Append a batch of ranged subclips to the current timeline. |

## `intelligence/color_assist.py`

| Tool | Description |
|---|---|
| `celavii_color_assist` | Get AI-powered color correction suggestions for the current frame. |
| `celavii_match_to_reference` | Match the current frame's color to a reference image using AI. |

## `intelligence/timeline_critique.py`

| Tool | Description |
|---|---|
| `celavii_suggest_markers` | AI analyzes the timeline and suggests where markers should be placed. |
| `celavii_timeline_critique` | Get AI editorial feedback on the current timeline. |
| `celavii_visual_continuity_check` | Check visual continuity by analyzing frames at each cut point. |

## `intelligence/vision.py`

| Tool | Description |
|---|---|
| `celavii_analyze_frame` | Analyze the current timeline frame using Gemini vision AI. |
| `celavii_compare_frames` | Compare the current frame to a reference image. |
| `celavii_describe_shot` | Get a cinematography-focused description of the current frame. |
| `celavii_frame_info` | Get metadata about the current frame without AI analysis. |
| `celavii_ocr_frame` | Extract text from the current frame using Gemini OCR. |

## `tools/color.py`

| Tool | Description |
|---|---|
| `celavii_add_node` | Add a new node to the node graph. |
| `celavii_apply_grade_from_drx` | Apply a grade from a .drx file to timeline items. |
| `celavii_copy_grades` | Copy color grades from one timeline item to others. |
| `celavii_get_cdl` | Get the CDL values of a timeline item. |
| `celavii_get_lut` | Get the LUT path applied to a node. |
| `celavii_get_node_graph` | Get the node graph for a timeline item. |
| `celavii_grab_still` | Grab a still from the current frame in the timeline. |
| `celavii_set_cdl` | Set CDL values on a timeline item. |
| `celavii_set_lut` | Apply a LUT to a color node. |
| `celavii_set_node_cache_mode` | Set the cache mode on a color node. |
| `celavii_set_node_enabled` | Enable or disable a color node. |
| `celavii_set_node_label` | Set the label on a color node. |

## `tools/fairlight.py`

| Tool | Description |
|---|---|
| `celavii_get_audio_track_info` | Get information about all audio tracks in the current timeline. |
| `celavii_insert_audio_at_playhead` | Insert an audio file at the playhead position on the current audio track. |
| `celavii_set_audio_track_volume` | Set the volume of an audio track. |
| `celavii_voice_isolation` | Apply voice isolation to an audio track (Studio only). |

## `tools/fusion.py`

| Tool | Description |
|---|---|
| `celavii_add_fusion_comp` | Add a new Fusion composition to a timeline item. |
| `celavii_delete_fusion_comp` | Delete a Fusion composition by name. |
| `celavii_export_fusion_comp` | Export a Fusion composition to a .comp file. |
| `celavii_fusion_add_tool` | Add a tool/node to a Fusion composition. |
| `celavii_fusion_connect` | Connect two tools in a Fusion composition. |
| `celavii_fusion_delete_tool` | Delete a tool from a Fusion composition. |
| `celavii_fusion_end_undo` | End an undo group in a Fusion composition. |
| `celavii_fusion_find_tool` | Find a tool in a Fusion composition by name. |
| `celavii_fusion_get_comp_info` | Get information about a Fusion composition (frame range, current time). |
| `celavii_fusion_get_input` | Get the current value of an input on a Fusion tool. |
| `celavii_fusion_get_tool_list` | List all tools in a Fusion composition. |
| `celavii_fusion_render` | Render a Fusion composition. |
| `celavii_fusion_set_input` | Set an input value on a Fusion tool. |
| `celavii_fusion_undo` | Start an undo group in a Fusion composition. |
| `celavii_get_fusion_comp_count` | Get the number of Fusion compositions on a timeline item. |
| `celavii_import_fusion_comp` | Import a Fusion composition from a .comp file. |
| `celavii_insert_fusion_comp_into_timeline` | Insert a new Fusion composition clip at the playhead in the timeline. |
| `celavii_load_fusion_comp` | Load/activate a Fusion composition by name. |
| `celavii_rename_fusion_comp` | Rename a Fusion composition. |

## `tools/gallery.py`

| Tool | Description |
|---|---|
| `celavii_delete_stills` | Delete stills from the current album. |
| `celavii_export_stills` | Export stills from the current album to files. |
| `celavii_get_current_album` | Get the currently selected still album. |
| `celavii_import_stills` | Import stills into the current album. |
| `celavii_list_gallery_albums` | List all still albums in the gallery. |
| `celavii_list_power_grade_albums` | List all power grade albums. |
| `celavii_list_stills` | List all stills in the current album. |
| `celavii_set_current_album` | Set the current still album by index. |
| `celavii_set_still_label` | Set the label on a still in the current album. |

## `tools/graph.py`

| Tool | Description |
|---|---|
| `celavii_add_group_graph_node` | Add a node to a color group's pre/post-clip graph. |
| `celavii_create_color_group` | Create a new color group. |
| `celavii_delete_color_group` | Delete a color group by name. |
| `celavii_get_color_group_clips` | Get all timeline items assigned to a color group. |
| `celavii_get_post_clip_graph` | Get the post-clip node graph for a color group. |
| `celavii_get_pre_clip_graph` | Get the pre-clip node graph for a color group. |
| `celavii_set_group_graph_lut` | Apply a LUT to a node in a color group's pre/post-clip graph. |

## `tools/interchange.py`

| Tool | Description |
|---|---|
| `celavii_export_aaf` | Export the current timeline as AAF (Advanced Authoring Format). |
| `celavii_export_csv` | Export the current timeline as CSV. |
| `celavii_export_edl` | Export the current timeline as an EDL (Edit Decision List). |
| `celavii_export_fcpxml` | Export the current timeline as FCPXML (Final Cut Pro XML). |
| `celavii_export_otio` | Export the current timeline as OTIO (OpenTimelineIO). |
| `celavii_import_timeline_file` | Import a timeline from a file (AAF, EDL, XML, FCPXML, OTIO). |

## `tools/layout.py`

| Tool | Description |
|---|---|
| `celavii_delete_layout_preset` | Delete a layout preset. |
| `celavii_export_burn_in_preset` | Export a burn-in preset to a file. |
| `celavii_export_layout_preset` | Export a layout preset to a file. |
| `celavii_get_keyframe_mode` | Get the current keyframe mode. |
| `celavii_import_burn_in_preset` | Import a burn-in preset from a file. |
| `celavii_import_layout_preset` | Import a layout preset from a file. |
| `celavii_load_layout_preset` | Load a UI layout preset. |
| `celavii_refresh_lut_list` | Refresh the project's LUT list (re-scan LUT directories). |
| `celavii_save_layout_preset` | Save the current UI layout as a preset. |
| `celavii_set_keyframe_mode` | Set the keyframe mode. |
| `celavii_update_layout_preset` | Update an existing layout preset with the current UI state. |

## `tools/lut_registry.py`

| Tool | Description |
|---|---|
| `celavii_get_lut_install_guide` | Get step-by-step instructions for installing the LUT for a specific camera. |
| `celavii_install_lut_file` | Install a downloaded LUT file into the Celavii LUT library. |
| `celavii_lut_library_status` | Show the status of the LUT library — what's installed and what's available. |
| `celavii_scan_lut_folder` | Scan the Resolve LUT folders and show all installed LUTs. |

## `tools/markers.py`

| Tool | Description |
|---|---|
| `celavii_add_clip_marker` | Add a marker to a media pool clip. |
| `celavii_add_item_marker` | Add a marker to a timeline item (clip marker, frame offset relative to clip start). |
| `celavii_add_timeline_marker` | Add a marker to the current timeline. |
| `celavii_delete_item_marker_at_frame` | Delete a marker on a timeline item at a specific frame offset. |
| `celavii_delete_item_markers_by_color` | Delete all markers of a specific color on a timeline item. |
| `celavii_delete_timeline_marker_at_frame` | Delete a timeline marker at a specific frame. |
| `celavii_delete_timeline_markers_by_color` | Delete all timeline markers of a specific color. |
| `celavii_get_clip_markers` | Get all markers on a media pool clip. |
| `celavii_get_item_markers` | Get all markers on a timeline item. |
| `celavii_get_timeline_markers` | Get all markers on the current timeline. |
| `celavii_update_timeline_marker_custom_data` | Update the custom data on a timeline marker. |

## `tools/media_pool.py`

| Tool | Description |
|---|---|
| `celavii_clear_clip_color` | Clear the clip color label from a media pool clip. |
| `celavii_create_bin` | Create a new bin (folder) in the media pool. |
| `celavii_delete_bins` | Delete bins by name. Bins must be empty. |
| `celavii_delete_clips` | Delete clips from the media pool by name. |
| `celavii_export_metadata` | Export media pool metadata to a CSV file. |
| `celavii_get_clip_info` | Get all properties and metadata for a media pool clip. |
| `celavii_get_current_bin` | Get the currently selected bin in the media pool. |
| `celavii_import_media` | Import media files into the media pool. |
| `celavii_link_proxy_media` | Link proxy media to a clip. |
| `celavii_list_bins` | List all bins (folders) in the media pool with clip counts. |
| `celavii_list_clips` | List clips in a bin. Defaults to the current bin. |
| `celavii_move_bins` | Move bins to a different parent bin. |
| `celavii_move_clips` | Move clips to a different bin. |
| `celavii_refresh_bins` | Refresh the media pool folder structure. |
| `celavii_relink_clips` | Relink clips to a new media path. |
| `celavii_replace_clip` | Replace a clip's media with a different file. |
| `celavii_search_clips` | Search for clips by name substring (case-insensitive). |
| `celavii_set_clip_color` | Set the clip color label on a media pool clip. |
| `celavii_set_clip_metadata` | Set a metadata field on a media pool clip. |
| `celavii_set_clip_property` | Set a property on a media pool clip. |
| `celavii_set_current_bin` | Set the current bin by name or path (e.g. 'Master/Footage/Day1'). |
| `celavii_unlink_clips` | Unlink clips from their media files (make offline). |
| `celavii_unlink_proxy_media` | Unlink proxy media from a clip. |

## `tools/media_storage.py`

| Tool | Description |
|---|---|
| `celavii_browse_storage` | List sub-folders in a media storage path. |
| `celavii_import_from_storage` | Import media files from storage directly into the media pool. |
| `celavii_list_files_in_storage` | List files in a media storage path. |
| `celavii_list_volumes` | List all mounted media storage volumes. |
| `celavii_reveal_in_storage` | Reveal a file or folder in the Media Storage browser. |

## `tools/project.py`

| Tool | Description |
|---|---|
| `celavii_archive_project` | Archive a project to a .dra file with optional media. |
| `celavii_close_project` | Close the current project (returns to Project Manager). |
| `celavii_create_project` | Create a new project with the given name. |
| `celavii_create_project_folder` | Create a new folder in the current Project Manager location. |
| `celavii_delete_project` | Delete a project by name. The project must not be currently open. |
| `celavii_delete_project_folder` | Delete a folder in the Project Manager. Must be empty. |
| `celavii_export_project` | Export a project to a .drp file. |
| `celavii_get_current_database` | Get the currently active database. |
| `celavii_get_current_project` | Get the name and details of the currently open project. |
| `celavii_get_project_setting` | Get a project setting by key, or all settings if key is empty. |
| `celavii_get_version` | Get DaVinci Resolve product name, version, and current page. |
| `celavii_goto_parent_folder` | Navigate up one level in the Project Manager. |
| `celavii_goto_root_folder` | Navigate to the root of the Project Manager. |
| `celavii_import_project` | Import a project from a .drp file. |
| `celavii_list_databases` | List all available databases. |
| `celavii_list_project_folders` | List folders in the current Project Manager location. |
| `celavii_list_projects` | List all projects in the current database folder. |
| `celavii_open_project` | Open an existing project by name. |
| `celavii_open_project_folder` | Navigate into a Project Manager folder. |
| `celavii_restore_project` | Restore a project from a .dra archive. |
| `celavii_save_project` | Save the current project. |
| `celavii_set_project_setting` | Set a project setting. |
| `celavii_switch_database` | Switch to a different database. |
| `celavii_switch_page` | Switch to a Resolve page. |

## `tools/render.py`

| Tool | Description |
|---|---|
| `celavii_add_render_job` | Add the current timeline to the render queue with current settings. |
| `celavii_delete_all_render_jobs` | Clear all render jobs from the queue. |
| `celavii_delete_render_job` | Delete a render job from the queue. |
| `celavii_export_render_preset` | Export a render preset to a file. |
| `celavii_get_render_codecs` | List available codecs for a render format. |
| `celavii_get_render_format_and_codec` | Get the current render format and codec. |
| `celavii_get_render_formats` | List all available render formats. |
| `celavii_get_render_job_status` | Get the status of a specific render job. |
| `celavii_get_render_jobs` | List all render jobs in the queue. |
| `celavii_get_render_resolutions` | List available resolutions for a format/codec combination. |
| `celavii_get_render_settings` | Get all current render settings. |
| `celavii_import_render_preset` | Import a render preset from a file. |
| `celavii_is_rendering` | Check if rendering is currently in progress. |
| `celavii_list_render_presets` | List all available render presets. |
| `celavii_load_render_preset` | Load a render preset by name. |
| `celavii_save_render_preset` | Save current render settings as a new preset. |
| `celavii_set_render_format_and_codec` | Set the current render format and codec. |
| `celavii_set_render_settings` | Set render settings. |
| `celavii_start_render` | Start rendering jobs in the queue. |
| `celavii_stop_render` | Stop the current render. |

## `tools/scripting.py`

| Tool | Description |
|---|---|
| `celavii_execute_lua` | Execute a Lua script in the Fusion environment. |
| `celavii_execute_python` | Execute arbitrary Python code with DaVinci Resolve API objects in scope. |

## `tools/timeline_edit.py`

| Tool | Description |
|---|---|
| `celavii_create_compound_clip` | Create a compound clip from selected timeline items. |
| `celavii_create_fusion_clip` | Create a Fusion clip from selected timeline items. |
| `celavii_get_item_property` | Get a property from a timeline item. |
| `celavii_insert_fusion_generator` | Insert a Fusion generator into the current timeline at the playhead. |
| `celavii_insert_fusion_title` | Insert a Fusion title into the current timeline at the playhead. |
| `celavii_insert_generator` | Insert a generator into the current timeline at the playhead. |
| `celavii_insert_title` | Insert a title into the current timeline at the playhead. |
| `celavii_list_timeline_items` | List all items on a timeline track. |
| `celavii_set_clip_enabled` | Enable or disable a timeline item. |
| `celavii_set_composite_mode` | Set the composite/blend mode on a timeline item. |
| `celavii_set_crop` | Set crop values on a timeline item. |
| `celavii_set_item_property` | Set a property on a timeline item. |
| `celavii_set_opacity` | Set the opacity of a timeline item (0.0 to 100.0). |
| `celavii_set_speed` | Set the playback speed of a timeline item. |
| `celavii_set_transform` | Set transform properties on a timeline item in one call. |

## `tools/timeline_items.py`

| Tool | Description |
|---|---|
| `celavii_add_item_flag` | Add a flag to a timeline item. |
| `celavii_add_version` | Add a new grade version to a timeline item. |
| `celavii_assign_to_color_group` | Assign a timeline item to a color group. |
| `celavii_clear_item_flags` | Clear flags from a timeline item. |
| `celavii_delete_version` | Delete a grade version. |
| `celavii_finalize_take` | Finalize the current take (remove other takes, keep selected). |
| `celavii_get_current_video_item` | Get the timeline item currently under the playhead. |
| `celavii_get_item_flags` | Get all flags on a timeline item. |
| `celavii_get_playhead_position` | Get the current playhead timecode. |
| `celavii_list_color_groups` | List all color groups in the current project. |
| `celavii_list_takes` | List all takes on a timeline item. |
| `celavii_list_versions` | List all grade versions on a timeline item. |
| `celavii_load_version` | Switch to a specific grade version. |
| `celavii_rename_version` | Rename a grade version. |
| `celavii_select_take` | Select a specific take on a timeline item. |
| `celavii_set_item_clip_color` | Set the clip color on a timeline item. |
| `celavii_set_playhead_position` | Set the playhead to a specific timecode. |

## `tools/timeline_mgmt.py`

| Tool | Description |
|---|---|
| `celavii_add_track` | Add a track to the current timeline. |
| `celavii_append_clips_to_timeline` | Append clips from the media pool to the end of the current timeline. |
| `celavii_create_timeline` | Create a new empty timeline. |
| `celavii_create_timeline_from_clips` | Create a timeline populated with the specified clips. |
| `celavii_delete_timelines` | Delete timelines by name. |
| `celavii_delete_track` | Delete a track from the current timeline. |
| `celavii_duplicate_timeline` | Duplicate the current timeline. |
| `celavii_export_timeline` | Export the current timeline to a file. |
| `celavii_get_current_timeline` | Get info about the currently active timeline. |
| `celavii_get_track_count` | Get the number of tracks of a given type. |
| `celavii_get_track_name` | Get the name of a track. |
| `celavii_import_timeline` | Import a timeline from a file (AAF, EDL, XML, FCPXML, OTIO). |
| `celavii_list_timelines` | List all timelines in the current project. |
| `celavii_set_current_timeline` | Switch to a timeline by name. |
| `celavii_set_timeline_name` | Rename the current timeline. |
| `celavii_set_track_enabled` | Enable or disable a track. |
| `celavii_set_track_lock` | Lock or unlock a track. |
| `celavii_set_track_name` | Rename a track. |

## `workflows/assembly.py`

| Tool | Description |
|---|---|
| `celavii_add_clips_to_track` | Add specific clips to a specific track on the current timeline. |
| `celavii_assembly_from_bin` | Create a timeline from all clips in a bin with track configuration. |
| `celavii_quick_assembly` | Create a timeline and add clips to it in one step. |

## `workflows/chroma_key.py`

| Tool | Description |
|---|---|
| `celavii_chroma_key` | Apply chroma key (green/blue screen removal) to timeline clips via Fusion DeltaKeyer. |

## `workflows/conform.py`

| Tool | Description |
|---|---|
| `celavii_conform_timeline` | Import a timeline file and relink to source media. |
| `celavii_relink_offline_clips` | Relink all offline clips in a bin to media at a new path. |
| `celavii_verify_timeline_media` | Verify that all clips in the current timeline are online. |

## `workflows/delivery.py`

| Tool | Description |
|---|---|
| `celavii_batch_deliver` | Queue multiple renders with different presets and start them all. |
| `celavii_quick_deliver` | One-command render: set format, configure output, queue, and start. |
| `celavii_render_status` | Check the status of all render jobs in the queue. |

## `workflows/grade.py`

| Tool | Description |
|---|---|
| `celavii_batch_apply_lut` | Apply a LUT to multiple timeline items at once. |
| `celavii_copy_grade_to_all` | Copy the grade from one clip to all other clips on the same track. |
| `celavii_list_cst_luts` | List all supported cameras and CST LUTs for the 6-node log grading workflow. |
| `celavii_quick_grade` | Apply a LUT and/or CDL to a timeline item in one step. |
| `celavii_setup_log_grade` | Set up the 6-node log footage grading structure on a clip or all clips. |

## `workflows/ingest.py`

| Tool | Description |
|---|---|
| `celavii_ingest_media` | Import media from a folder into the media pool with optional organisation. |
| `celavii_ingest_with_bins` | Import media and auto-organise into bins based on folder structure. |
